# grep command
[[linux]]

- -w, match whole word
-  -H, rint the file name for each match.
- 
-  -n, --line-number
-  -r, --recursive
-  -i, --ignore-case
-  `grep -HrnIw 'text'`
-  `grep -HrnIw --include=*.js 'text'`
